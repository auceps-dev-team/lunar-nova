# Snippet CI pour les tests E2E (P2-4) — à coller comme job supplémentaire
# dans .github/workflows/ci.yml par l'équipe éditrice (le jeton GitHub App des
# contributions automatisées n'a pas la permission `workflows`, constat H2 :
# il ne peut pas modifier lui-même le workflow).
# L'équivalent YAML du job :

#  e2e:
#    runs-on: ubuntu-latest
#    needs: [build]  # adapter au nom du job existant
#    steps:
#      - uses: actions/checkout@v4
#      - uses: actions/setup-node@v4
#        with:
#          node-version: 20
#          cache: npm
#          cache-dependency-path: |
#            whatsapp-ai-saas/package-lock.json
#            whatsapp-ai-saas/backend/package-lock.json
#      - name: Installer xvfb
#        run: sudo apt-get update && sudo apt-get install -y xvfb libnss3 libatk-bridge2.0-0 libgtk-3-0
#      - name: Installer les dépendances (avec scripts => binaire Electron)
#        working-directory: whatsapp-ai-saas
#        run: npm ci && cd backend && npm ci
#      - name: Build du renderer
#        working-directory: whatsapp-ai-saas
#        run: npx vite build
#      - name: Tests E2E (xvfb)
#        working-directory: whatsapp-ai-saas
#        run: xvfb-run -a npm run test:e2e
